[project]
name = "gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "itsdangerous",
  "jinja2",
  "modal",
  "python-multipart",
  "python-dotenv",
]

[dependency-groups]
dev = ["pytest", "mypy", "ruff"]

[tool.uv]
package = false

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.mypy]
python_version = "3.12"
strict = false
